name: Backup n8n Workflows + DB

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch: {}

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955

      - name: Install tools
        run: |
          sudo apt-get update
          sudo apt-get install -y postgresql-client jq

      - name: Run backup script
        env:
          # n8n API
          N8N_BASE_URL: ${{ secrets.N8N_BASE_URL }}
          N8N_API_KEY: ${{ secrets.N8N_API_KEY }}
          # DB (optional) for pg_dump
          DB_POSTGRESDB_HOST: ${{ secrets.DB_HOST }}
          DB_POSTGRESDB_PORT: ${{ secrets.DB_PORT }}
          DB_POSTGRESDB_DATABASE: ${{ secrets.DB_NAME }}
          DB_POSTGRESDB_USER: ${{ secrets.DB_USER }}
          DB_POSTGRESDB_PASSWORD: ${{ secrets.DB_PASSWORD }}
        run: |
          bash scripts/backup.sh

      - name: Upload artifact (backups + workflow JSONs)
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: n8n-backup
          path: |
            backups/
            workflows/backup/
          retention-days: 30
