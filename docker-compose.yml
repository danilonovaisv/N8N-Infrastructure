version: "3.9"
services:
  n8n:
    image: n8nio/n8n:1.108.0
    restart: unless-stopped
    ports:
      - "5678:5678"
    environment:
      # ===== Base URL / Webhooks =====
      N8N_HOST: danilonovais-n8n-dan.hf.space
      N8N_PORT: 5678
      N8N_PROTOCOL: https
      WEBHOOK_URL: https://danilonovais-n8n-dan.hf.space/

      # ===== Pastas e persistência =====
      N8N_USER_FOLDER: /data/.n8n

      # ===== Segurança / Criptografia =====
      N8N_ENCRYPTION_KEY: ${N8N_ENCRYPTION_KEY}
      N8N_USER_MANAGEMENT_JWT_SECRET: ${N8N_USER_MANAGEMENT_JWT_SECRET}
      N8N_API_KEY: ${N8N_API_KEY}
      CSRF_SECRET: ${CSRF_SECRET}
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS}
      N8N_LOG_LEVEL: ${N8N_LOG_LEVEL:-info}
      N8N_METRICS: ${N8N_METRICS:-true}

      # ===== Banco (Supabase via TLS/CA) =====
      DB_TYPE: postgresdb
      DB_POSTGRESDB_CONNECTION_STRING: ${DB_POSTGRESDB_CONNECTION_STRING}
      DB_POSTGRESDB_SSL: "true"
      DB_POSTGRESDB_SSL_CA: /data/.n8n/ca.pem
      # Se, e somente se, o CA der erro na sua imagem, use temporariamente:
      # DB_POSTGRESDB_SSL_REJECT_UNAUTHORIZED: "false"

      # ===== Integrações (ex.: caminhos de credenciais) =====
      GOOGLE_CREDENTIALS_PATH: /data/.n8n/credentials/google-service-account.json

    volumes:
      - n8n_data:/data

    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:5678/rest/health"]
      interval: 15s
      timeout: 5s
      retries: 10

volumes:
  n8n_data:
